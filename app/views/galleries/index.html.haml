- model_class = Gallery

.span9
  = render_breadcrumbs
  .well
    .navbar.navbar-heading
      .navbar-inner
        .container
          %span.brand= t '.title', :default => model_class.model_name.human.pluralize
          = link_to raw('<i class="icon-plus-sign"></i> ' + t('.new', :default => t("helpers.links.new"))), new_gallery_path, :class => 'btn btn-success btn-mini btn-plus pull-right'

    = render 'galleries'

.span3= render 'sidebar'